board = []  # Initialize an empty list for the board
for i in range(3):
    row = ['_'] * 3  # Create a row with 3 underscores
    board.append(row)  # Add the row to the board

# Print the board row by row
for row in board:
    print(row)

